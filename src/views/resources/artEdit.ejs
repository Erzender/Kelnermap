<div
  style="margin-left: auto; margin-right: auto; width: 1000px; max-width: 100%;"
>
  <h1 style="color: purple;">✒️<%= fields.title %></h1>
  Un pixel art sur carte, un livre, une statue, ... ? On veut tout savoir.
  <form id="fart" action="/lekelner/explorer/oeuvres/editeur" method="post">
    <input name="id" style="display: none" value="<%= fields.id %>" />
    <input name="title" style="display: none" value="<%= fields.title %>" />
    <p>🏷️Nom <input name="name" value="<%= fields.name %>" /></p>
    <p>
        <div>📚Description</div>
        <textarea rows="10" cols="50" height="400px" name="description" form="fart" ><%= fields.description %></textarea>
    </p>
    <p>🖼️Image <input name="picture" value="<%= fields.picture %>" /></p>
    <p>
      🏠Edifice d'exposition : <select name="edifice" form="fart" >
            <% edifices.forEach(function(edifice){ %>
                <option <%= edifice.id === fields.edifice ? 'selected' : '' %> value="<%= edifice.id %>"><%= edifice.name %></option>
            <% }); %>
        </select>
    </p>
    <p>
      💬Lien du post mastodon pour les commentaires <input name="mastodon" value="<%= fields.mastodon %>" />
    </p>
    <% if (fields.id > 0) { %>
      <p style="color: red;">🗑️<input name="delete" type="checkbox" /> Supprimer l'oeuvre ?</p>
    <% } %>
    <button type="submit">Générer la commande Discord 🖥</button>
  </form>
  <% if (fields.command.length > 0) { %>
    <textarea cols="50" readonly onClick="this.select();" ><%= fields.command %></textarea>
  <% } %>
</div>
